spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5433/postgres
    username: postgres
    password: postgres123
  jpa:
    show-sql: true

logging:
  level:
    com.guilda: DEBUG
